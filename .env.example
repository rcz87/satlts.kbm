# ================================================
# PORTAL SATLANTAS POLRES KEBUMEN
# Environment Variables Template untuk VPS Deployment
# ================================================
#
# INSTRUKSI:
# 1. Copy file ini menjadi .env
# 2. Isi semua nilai dengan data yang sebenarnya
# 3. JANGAN COMMIT file .env ke Git (sudah ada di .gitignore)
#

# ================================================
# DATABASE CONFIGURATION
# ================================================
# Format: postgresql://username:password@host:port/database_name
# Contoh: postgresql://satlantas:mypassword@localhost:5432/satlantas_db
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# PostgreSQL Connection Details (optional, jika tidak menggunakan DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=satlantas
PGPASSWORD=your_database_password
PGDATABASE=satlantas_db

# ================================================
# FLASK SECRET KEY
# ================================================
# Generate random secret key dengan command:
# python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=generate-random-secret-key-here-min-32-characters

# ================================================
# FLASK CONFIGURATION
# ================================================
FLASK_ENV=production
FLASK_DEBUG=False

# ================================================
# SERVER CONFIGURATION (Optional)
# ================================================
# Host dan Port untuk aplikasi (default: 0.0.0.0:8000 dengan Gunicorn)
# HOST=0.0.0.0
# PORT=8000

# ================================================
# DOMAIN CONFIGURATION (Optional)
# ================================================
# Domain utama untuk aplikasi
# SERVER_NAME=satlantas.polreskebumen.go.id

# ================================================
# CATATAN DEPLOYMENT
# ================================================
# 1. DATABASE_URL: Pastikan PostgreSQL sudah terinstall dan database sudah dibuat
# 2. SESSION_SECRET: WAJIB diubah untuk security, generate random string yang kuat
# 3. Untuk production, pastikan FLASK_DEBUG=False
# 4. Jaga kerahasiaan file .env, jangan share atau commit ke repository
